package Spring.MongoSpring;

import java.util.HashSet;

class App
{
	public static void main(String[] args) {
		
		GofroDataToCollection obj = new GofroDataToCollection();
		MongoDbService service = new MongoDbService();		
		HashSet<State> states = obj.createStatesList("states.csv", service);
		for(State i: states)
		{
			i.diplayState();
		}
		
		if(service.writeState(states))
			System.out.println(Success);;


		
		service.getCountryByName("India").displayCountry();
		
		
		
	}
}